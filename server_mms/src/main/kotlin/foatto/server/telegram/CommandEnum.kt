package foatto.server.telegram

enum class CommandEnum(val text: String) {
    START("/start"),
//    LOGON("/logon"),      - command with parameters example
//    BUTTONS("/buttons"),  - button commands example
}